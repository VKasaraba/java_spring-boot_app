spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kasaraba_lab5?serverTimezone=UTC
    username: volodymyr
    password: kohavyna2017
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    # Show or not log for each sql query
    show-sql: true
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: update
      # Naming strategy
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"

